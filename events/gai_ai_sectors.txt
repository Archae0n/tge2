namespace = gai_ai_sectors

country_event = {
	id = gai_ai_sectors.1	
	hide_window = yes
	#is_triggered_only = yes
	#fire_only_once = yes
	
	mean_time_to_happen = {
		years = 5
	}
	
	trigger = {
		is_ai = yes
		has_technology = tech_starbase_3
		NOT = { has_global_flag = disable_gai_ai_sectors }
		NOT = { has_modifier = ai_sectors_only }
	}

	immediate = {
		add_modifier = {
			modifier = ai_sectors_only
			days = -1
		}
	}
}
